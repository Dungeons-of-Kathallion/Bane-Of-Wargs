name: str()
type: str()
cost value: num()
description: str()
news: list(str())
map:
  map raw: str(required=False)
  map full: str()
forge:
  buys: list(str())
  sells: list(str())
discounts:
  time space: num()
  chance: num()
  discount:
    max dropoff: num()
    min dropoff: num()
