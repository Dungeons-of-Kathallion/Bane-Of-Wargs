name: str()
type: str()
cost value: num()
sleep gold: num()
description: str()
news: list(str())
sells:
  drinks: list(str())
  items: list(str())
buys:
  items: list(str())
map:
  map raw: str(required=False)
  map full: str()
discounts:
  time space: num()
  chance: num()
  discount:
    max dropoff: num()
    min dropoff: num()
