name: str()
type: str()
cost value: num()
description: str()
news: list(str())
map:
  map raw: str(required=False)
  map full: str()
blacksmith:
  buys: list(str())
  orders: any()
discounts:
  time space: num()
  chance: num()
  discount:
    max dropoff: num()
    min dropoff: num()
