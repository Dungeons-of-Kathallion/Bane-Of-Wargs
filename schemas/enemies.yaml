plural: str()
never run away: bool(required=False)
damage:
  min damage: int()
  max damage: int()
  critical chance: num()
  one shot: bool(required=False)
health:
  min spawning health: int()
  max spawning health: int()
  max health level: int()
agility: num()
inventory: list(str())
description: str()
