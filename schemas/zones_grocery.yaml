name: str()
type: str()
description: str()
news: list(str())
map:
  map raw: str(required=False)
  map full: str()
cost value: num()
items sold: list(str())
discounts:
  time space: num()
  chance: num()
  discount:
    max dropoff: num()
    min dropoff: num()
